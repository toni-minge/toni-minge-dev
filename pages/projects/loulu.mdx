import Layout from '../../components/layout/layout'

export const meta = {
  author: 'Rich Haines',
  headerImage: '/img/header-about-me.jpg'
}

# Loulu

This is the body of my blog post.

export default ({ children }) => <Layout optionalHeader={true} meta={meta}>{children}</Layout>
